1. Let us consider the following function $f (x, y) = x^2y + \sin(xy)$:

(a) Find the gradient $\nabla f (x, y) := (f_x, f_y )^\top $.

$$(2xy+y\cos(xy),x^2+x\cos(xy))$$

(b) Find the directional derivative of $f (x, y)$ at $(1, \pi)$ in the direction of the vector $v = (3, 4)^\top$ (Hint: recall that the directional derivative in the direction of the vector $v$ is $v^\top \cdot\nabla f$.

$$$$

(c) Find a unit vector $v$ such that the directional derivative of $f (x, y)$ at $(1, \pi)$ vanishes (is equal to 0). Explain what it means geometrically
    
$$$$